sudo: required
services:
    - docker

before_install:
    - docker build -t stevenu/docker-react -f dockerfile.dev .

script:
    - docker run stevenu/docker-react npm run test -- --coverage

language: 
    - generic 